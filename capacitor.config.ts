import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.firebaseapptest',
  appName: 'firebaseapptest',
  webDir: 'www/browser'
};

export default config;
